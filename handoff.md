# 交接檔 (handoff.md)

> 任何 Agent、任何電腦接手，**必須**先讀取本檔；收工時**必須**更新。本檔只留交接、精簡資訊，詳細脈絡寫 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
- **刪除單據防復活墓碑機制（Tombstone Blacklist）**：建立 `local_repair_deleted_ids_v1` 本地持久化黑名單，在初始化、資料合併（`mergeTickets`）、讀取（`fetchData`）與雲端寫入時全面排除已刪除單據，杜絕刪除後重新整理頁面卡片又被雲端舊資料復活的問題。
- **連續同步排隊保證（hasPendingSync Queue）**：修復先前在進行同步期間（`isSyncing === true`）使用者若立即刪除卡片，後續同步被直接 drop 遺失的重大漏洞。現在任何在同步期間產生的變更皆會標記排隊，並在前次完成後自動補發最新狀態。
- **刪除操作即時強制同步**：刪除單據不經 500ms 防抖延遲，立即觸發即時寫入雲端。
- **頂部工具列精簡與重複卡片防護**：移除冗餘「同步寫入雲端」按鈕；單據 ID 去重保護；GAS 心跳保溫機制。

## 🚦 目前狀態
- 前端程式碼（`index.html`、`engineer.html`、`app.js`）全數通過語法校驗並成功推送至 GitHub `master`。
- Vercel 線上站點已完成即時自動部署。

## ➡️ 下一步
1. 請使用者使用 `Ctrl + F5` 重新整理頁面，測試新建單據後立即刪除，並再次重新整理確認是否不再復活。
2. 進行日常看板拖曳與派單維護測試。

## ⚠️ 注意事項
- 瀏覽器端如有舊快取，務必強制重新整理 `Ctrl + F5`。

## 🕐 最後更新
- 更新時間：2026-09-17 23:28
- 更新者：阿噗 @ Windows
- Git push：✅ 已推 (`master`)
