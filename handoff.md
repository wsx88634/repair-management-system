# 交接檔 (handoff.md)

> 任何 Agent、任何電腦接手，**必須**先讀取本檔；收工時**必須**更新。本檔只留交接、精簡資訊，詳細脈絡寫 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
- **頂部排版全面重整與名稱更新**：左側標題正式修改為「叫修輔助管理面板」，並設定 `whitespace-nowrap` 防折行；右側 8 顆功能按鈕透過間距微調與消除重複 emoji，完美實現全按鈕同一水平列等距整齊對齊（「擷取看板」不再孤單折行）。
- **刪除單據防復活墓碑機制（Tombstone Blacklist）**：建立 `local_repair_deleted_ids_v1` 本地持久化黑名單，在初始化、資料合併（`mergeTickets`）、讀取（`fetchData`）與雲端寫入時全面排除已刪除單據，杜絕刪除後重新整理頁面卡片又被雲端舊資料復活的問題。
- **背景靜默即時同步與連續同步排隊保證（hasPendingSync Queue）**：刪除操作改為背景靜默同步，卡片瞬間移除且頂部絕不跳出「同步中」旋轉圖示干擾；同時實作排隊機制，確保非同步寫入期間任何操作絕不漏發。
- **後端讀取鎖定瓶頸解除與連線狀態標籤智慧優化**：更新 `gas/Code.gs` 移除 `doGet` 的排它鎖，心跳 ping 秒回；前端狀態燈號在連線期間明確顯示「雲端連線中...」，不再誤閃「本地體驗模式」。

## 🚦 目前狀態
- 前端程式碼（`index.html`、`engineer.html`、`app.js`）與後端 `gas/Code.gs` 全數更新完畢，並通過全線語法驗證。
- 已推送至 GitHub `master` 分支，Vercel 線上站點即時自動更新完成。

## ➡️ 下一步
1. 驗證頂部功能按鈕群與「叫修輔助管理面板」在各螢幕解析度下的水平對齊視覺效果。
2. 日常叫修進件、派工拖曳、時間軸紀錄與單據刪除之流轉驗證。

## ⚠️ 注意事項
- 瀏覽器端如有舊快取，建議按下 `Ctrl + F5` 載入最新排版與腳本。

## 🕐 最後更新
- 更新時間：2026-09-18 00:05 (收工)
- 更新者：阿噗 @ DESKTOP-U8HAOU6
- Git push：✅ 已推 (`master`)
