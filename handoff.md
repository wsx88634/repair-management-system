# 交接檔 (handoff.md)

> 任何 Agent、任何電腦接手，**必須**先讀取本檔；收工時**必須**更新。本檔只留交接、精簡資訊，詳細脈絡寫 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
- **雲端同步連線韌性加固與逾時防卡死機制**：
  1. **加入 12 秒 AbortController 嚴格逾時中斷**：徹底修復原生 `fetch` 在網路延遲或暫態封包遺失時無限期掛起（Hang）的問題，若超過 12 秒無回應自動中止並重試，絕不再讓介面永遠停在「同步中」與「雲端連線中...」。
  2. **無縫平滑降級為本地快取模式**：遇到暫態網路波動時自動切換為本地快取模式，不彈出侵入式錯誤框干擾操作，確保離線排單流暢。
  3. **狀態標籤支援點擊重試連線**：琥珀色狀態標籤明確標示「本地快取模式 (點擊重試)」，點擊即可手動發起快速重連。
  4. **API 設定新增「恢復官方預設 API」**：在後端 API 設定彈窗底部增加一鍵重設按鈕，方便隨時校正或修復網址。
  5. **雙端同步**：`index.html` 與 `engineer.html` 雙端全線更新。
- **派單詳細資訊彈窗欄位尺寸與佈局優化**：
  1. **附件圖片區塊高度減半且由左至右靠左排列**：虛線框垂直高度與內邊距大幅精簡（`min-h-[58px]`），縮圖調整為精巧比例（`w-24 h-16`）。
  2. **詳細備註與其他資訊欄位垂直高度增長 2 倍**：將備註 textarea 由 `min-h-[120px]` 提升為 `min-h-[240px]` 並設 `rows="8"`。
- **頂部排版全面重整與名稱更新**：左側標題修改為「叫修輔助管理面板」，按鈕全水平列等齊。
- **刪除單據防復活墓碑機制（Tombstone Blacklist）**：本地持久化黑名單杜絕刪除後重新整理卡片又復活問題。

## 🚦 目前狀態
- 前端程式碼（`index.html`、`engineer.html`）全數更新完畢，已通過全線語法驗證並推送到 GitHub `master`，Vercel 已完成部署。

## ➡️ 下一步
1. 請使用者在瀏覽器按 `Ctrl + F5` 強制重新整理，檢視頂部連線狀態燈號（點擊標籤亦可手動觸發連線測試）。
2. 若有特定網路問題，可隨時點擊「後端 API 設定」中的「恢復官方預設 API」進行一鍵校驗。

## ⚠️ 注意事項
- 瀏覽器端如有舊快取，建議按下 `Ctrl + F5` 強制重新整理載入最新排版與腳本。

## 🕐 最後更新
- 更新時間：2026-09-19 19:03
- 更新者：阿噗 @ DESKTOP-U8HAOU6
- Git push：✅ 已推 (`master`)


