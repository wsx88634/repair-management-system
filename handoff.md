# 交接檔 (handoff.md)

> 任何 Agent、任何電腦接手，**必須**先讀取本檔；收工時**必須**更新。本檔只留交接、精簡資訊，詳細脈絡寫 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
- **截圖文字裁切修復**：修正 `html2canvas` 在高解析度截圖時因字型 baseline 下偏與 `truncate` 導致的中文字尾（如「店」、「保養」）下半部被切除問題，導入多元 fallback 字型與 `onclone` 自動展開樣式。
- **單據活動歷程時間軸 (時間節點紀錄區)**：在派單詳細資訊視窗中實裝垂直視覺化時間軸，自動記錄「案件成立」、「派工異動」、「狀態變更 (另約/報價/完修)」、「報價狀態」與「封存/還原」。
- **截圖便利性強化**：依使用者回饋在時間軸區塊頂部新增「🏢 客戶名稱（與機型）」標籤，方便截圖時免上下滾動對照。
- **雙向合併與本地持久化加固**：加固 `mergeTickets` 邏輯，確保頁面重新整理時不被空值覆蓋，優先載入本地 `localStorage` 歷程。
- **GAS 後端第 15 欄支援**：更新 `gas/Code.gs`，擴充支援試算表儲存 `logs` JSON 字串並相容 LINE Bot 自動進件記錄。

## 🚦 目前狀態
- 前端程式碼（`index.html`、`engineer.html`、`app.js`）皆已測試通過並發布至 GitHub `master` 分支。
- 本機端 `localStorage` 已完全具備活動歷程紀錄與持久化能力。
- 使用者已取得最新 `Code.gs` 程式碼與 3 步驟部署說明，隨時可至 Google Apps Script 建立新版本發布以開通雲端跨裝置同步。

## ➡️ 下一步
1. **GAS 雲端新版本部署**：使用者至 Google Apps Script 編輯器貼上最新 `Code.gs` 並執行「管理部署 ➔ 建立新版本 ➔ 部署」，完成跨裝置雲端儲存啟用。
2. **日常叫修作業與時間軸驗證**：在實際派工流轉中驗證看板拖曳指派與狀態推進之日誌完整度。

## ⚠️ 注意事項
- 舊有歷史單據若無新日誌，系統會智能根據既有的報修日期與完修日期自動生成基本歷程節點，無破壞性影響。

## 🕐 最後更新
- 更新時間：2026-09-03 00:33 (收工)
- 更新者：阿噗 @ Windows
- Git push：✅ 已推 (`master`)
