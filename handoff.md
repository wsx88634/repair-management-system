# 交接檔 (handoff.md)

> 任何 Agent、任何電腦接手，**必須**先讀取本檔；收工時**必須**更新。本檔只留交接、精簡資訊，詳細脈絡寫 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
- **派單詳細資訊彈窗欄位尺寸與佈局優化**：
  1. **附件圖片區塊高度減半且由左至右靠左排列**：虛線框垂直高度與內邊距大幅精簡（`min-h-[58px]`），無照片時改為單列精簡提示；有照片時改為 `justify-start flex-wrap gap-2.5` 由左至右依序排列，縮圖調整為精巧比例（`w-24 h-16`），讓整個附件區高度剛好縮減一半，既節省垂直視圖空間又整齊好閱覽。
  2. **詳細備註與其他資訊欄位垂直高度增長 2 倍**：將備註 textarea 由 `min-h-[120px]` 提升為 `min-h-[240px]` 並設 `rows="8"`，讓長篇叫修需求、聯絡人資訊、設備故障診斷能一目了然完整檢視與編輯。
  3. **雙端看板同步**：總管看板 `index.html` 與工程師看板 `engineer.html` 彈窗規格全線保持一致。
- **頂部排版全面重整與名稱更新**：左側標題正式修改為「叫修輔助管理面板」，並設定 `whitespace-nowrap` 防折行；右側 8 顆功能按鈕全水平列等齊。
- **刪除單據防復活墓碑機制（Tombstone Blacklist）**：本地持久化黑名單杜絕刪除後重新整理卡片又復活問題。

## 🚦 目前狀態
- 前端程式碼（`index.html`、`engineer.html`）全數更新完畢，並通過全線語法驗證。
- 已推送至 GitHub `master` 分支，Vercel 線上站點即時自動更新完成。

## ➡️ 下一步
1. 請使用者在瀏覽器按 `Ctrl + F5` 點開任一單據查看詳細資訊彈窗效果。
2. 依使用反饋持續微調日常派工與查看體驗。

## ⚠️ 注意事項
- 瀏覽器端如有舊快取，建議按下 `Ctrl + F5` 強制重新整理載入最新排版。

## 🕐 最後更新
- 更新時間：2026-09-19 18:47
- 更新者：阿噗 @ DESKTOP-U8HAOU6
- Git push：✅ 已推 (`master`)

